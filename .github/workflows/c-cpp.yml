name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v3
    - run: tclsh src/cssprop.tcl && tclsh src/tokenlist.txt && tclsh src/mkdefaultstyle.tcl > htmldefaultstyle.c 
    - run: chmod +rwx ./configure
    - run: mv *.c src && mv *.h src
    - name: Configure
      run: ls /usr/local/Cellar/tcl-tk/8.6.13
    - name: Configure
      run: ./configure CC="gcc" --with-tcl=/usr/local/Cellar/tcl-tk/8.6.13/lib --with-tk=/usr/local/Cellar/tcl-tk/8.6.13/lib --with-tclinclude=/usr/local/Cellar/tcl-tk/8.6.13/include --with-tkinclude=/usr/local/Cellar/tcl-tk/8.6.13/include
    - name: make
      run: make
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
